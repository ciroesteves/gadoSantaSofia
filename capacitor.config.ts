import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gadoSantaSofia',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
